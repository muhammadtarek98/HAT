build:
  cuda: "11.8"
  gpu: true
  python_version: "3.10"
  system_packages:
    - "libgl1-mesa-glx"
    - "libglib2.0-0"
  python_packages:
    - "numpy"
    - "ipython"
    - "einops"
  run:
    - pip install basicsr
predict: "predict.py:Predictor"
